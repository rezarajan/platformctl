feat(h9): Stage H criterion 3 composed — cross-domain deny/exempt/mediate/withdraw, Docker green; K8s leg code-complete

The composed scenario the Stage H audit demanded (docs/planning/08 H9):
every prior H-component (H3/H4 policy, H5 domains, H6/H10 mediation)
proven TOGETHER in one live 5-leg sequence, per docs/adr/021's 2026-07-23
severing amendment.

Scenario (testdata/crossdomain-mediated-scenario + -k8s-scenario, each
with policies/policy.yaml): a cdc Binding whose Source (domain payments,
co-located with the real postgres backend) feeds an EventStream (domain
analytics) through an openziti MediatedConnection; one exemptible
crossDomain deny rule. The topology yields two crossDomain edges over the
same domain pair (Binding sourceRef->targetRef, Source connectionRef->
Connection) — both exempted by annotation, both asserted by name in the
refusal legs. The Binding declares domain: analytics explicitly (ADR 022
addendum coherence — found live).

Tests: TestCrossDomainMediatedPolicyEndToEnd (Docker) and
TestOpenZitiCrossDomainPolicyOnKubernetesEndToEnd (named for the CI
scenarios-apps shard; partition guard green). Legs: (1) validate without
exemption refuses naming rule+domains+edges; (2) with exemption, apply ->
Ready, connector RUNNING through the mediated path; (3) POSITIVE mediator
evidence — Ziti management API's services/service-policies/identities
asserted EXACTLY (1 service, 1 datascape-mediated identity, 1 Dial policy
with exact @id roles); (4) exemption removed -> re-apply/plan REFUSED
fail-closed while the standing path keeps serving (status + connector
RUNNING); (5) Binding+Connection+Source removed -> apply (with the NFR-3
double flags the External Source's delete demands — the amendment's
"approved like any other destructive change") -> all three Ziti
collections empty. Cleanup via testkit.Janitor on both legs (ADR 029);
scripts/test-impact.sh gains the crossdomain-mediated row.

Three real defects found only by composing:

1. The H6 K8s addendum's recorded cross-domain bind-target gap, closed:
   new optional runtime.AddressQualifier capability
   (internal/ports/runtime/address.go), implemented ONLY by engine's
   domainRuntime — Docker/pinned/same-domain no-ops; K8s cross-domain
   bind targets qualified to <host>.<domain-ns>.svc.cluster.local:<port>.
   openziti consumes it via type-assertion with envelopes it already
   holds (zero domain symbols in the adapter; decoupling archtest green).
   Unit-pinned: TestDomainRuntimeQualifyTargetAddress.

2. H6's listDialPolicies filter (filter=type=%22Dial%22) is rejected by
   the pinned controller (HTTP 400 INVALID_FILTER — type is a numeric
   enum), so ObservedEdges/mediator drift detection was silently broken
   since H6; leg 3 caught it live. Fixed client-side-filter; pinned by
   TestListDialPoliciesFiltersClientSide. Distinct from H10's rewrite of
   the same file (verified against main before keeping).

3. Leg-5 teardown of the External Source requires the NFR-3 double
   flags — recorded as the amendment's approval mechanism, not bypassed.

Live evidence: Docker leg PASS 26.73s (apply 25s), zero residue.
Kubernetes leg: code-complete but NOT live-verified — the minted RBAC
kubeconfig token expired mid-session (auth can-i yes minutes earlier,
Unauthorized at run time); not re-minted per task rule. Stage criterion
3's box left UNCHECKED (Accept demands both runtimes green); doc 08
Done-note records the single remaining step.

Verification: gofmt clean; go build clean; go vet (plain + integration)
clean; go test ./... true-exit=0; golangci-lint v2.12.2 0 issues;
archtests green (shard partition, test-impact completeness, domain
decoupling, wrapper completeness, mediation layering).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
