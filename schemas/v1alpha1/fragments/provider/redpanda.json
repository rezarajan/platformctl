{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://datascape.io/schemas/fragments/provider/redpanda.json",
  "title": "Provider(type: redpanda).spec.configuration",
  "description": "Shape-only fragment (docs/planning/08 E5): mutual-exclusion rules between brokers and the host-port pins, and between brokers and schemaRegistry, remain SpecValidator checks (cross-field, not expressed here).",
  "type": "object",
  "properties": {
    "image": { "type": "string", "minLength": 1 },
    "kafkaPort": { "type": "integer", "minimum": 1, "maximum": 65535 },
    "adminPort": { "type": "integer", "minimum": 1, "maximum": 65535 },
    "schemaRegistryPort": { "type": "integer", "minimum": 1, "maximum": 65535 },
    "schemaRegistry": { "type": "string", "enum": ["enabled", "disabled"] },
    "brokers": { "type": "integer", "minimum": 1 }
  },
  "additionalProperties": false
}
