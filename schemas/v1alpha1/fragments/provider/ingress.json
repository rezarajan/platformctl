{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://datascape.io/schemas/fragments/provider/ingress.json",
  "title": "Provider(type: ingress).spec.configuration",
  "description": "Shape-only fragment (docs/planning/08 E5). port/adminPort are Docker-runtime-only (ignored on Kubernetes, docs/planning/08 C7) — a runtime-conditional refusal, if ever added, would be a SpecValidator/engine concern, not schema shape.",
  "type": "object",
  "properties": {
    "image": { "type": "string", "minLength": 1 },
    "domain": { "type": "string", "minLength": 1 },
    "port": { "type": "integer", "minimum": 1, "maximum": 65535 },
    "adminPort": { "type": "integer", "minimum": 1, "maximum": 65535 }
  },
  "additionalProperties": false
}
