apiVersion: datascape.io/v1alpha1
kind: Source
metadata:
  name: app-db
spec:
  engine: postgres
  providerRef:
    name: db
  postgres:
    database: appdb
    # schema omitted: defaults to "public" (docs/planning/08 E2).
