# platformctl stream-basics blueprint.
#
# This blueprint declares no SecretReference — a bare Redpanda broker plus
# topics needs no credentials — so there is nothing to fill in here. The
# file exists so every blueprint has the same `--env-file .env` habit;
# `platformctl apply --env-file .env` is a harmless no-op against an empty
# file.
