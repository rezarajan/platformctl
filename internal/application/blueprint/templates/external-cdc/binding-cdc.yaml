apiVersion: datascape.io/v1alpha1
kind: Binding
metadata:
  name: source-to-events
spec:
  mode: cdc
  sourceRef:
    name: source
  targetRef:
    name: change-events
  providerRef:
    name: cdc
  options:
    tables: [records]
    snapshotMode: initial
