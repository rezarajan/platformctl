# platformctl external-cdc blueprint — secrets for the `env` SecretStore
# backend. Fill in the real credentials for the external database this
# blueprint's Connection (connection.yaml) points at.
#
#   platformctl apply --env-file .env
#
# Any key left unset is reported by name at `apply` time (secret Preflight
# runs before any infrastructure is touched).

DATASCAPE_SECRET_EXT_DB_CREDS_USERNAME=replicator
DATASCAPE_SECRET_EXT_DB_CREDS_PASSWORD=change-me
