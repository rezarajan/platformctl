# The Debezium Kafka Connect worker realizing Binding(mode: cdc) below.
# Its secretRefs lists the external Connection's own credentials
# (ext-db-creds) — the CDC provider resolves the source database's
# address from the Source's Connection automatically; only credentials
# need to be declared here. spec.configuration.bootstrapServers is
# similarly omitted — inferred from the manifest graph, since the CDC
# Binding below targets change-events, whose Provider is broker
# (docs/planning/08 E2).
apiVersion: datascape.io/v1alpha1
kind: Provider
metadata:
  name: cdc
  # docs/adr/021 zero-trust pack (H4): blueprint uses the default pinned
  # image, not a registry.corp.* one — waived, not applicable outside this
  # dev-flavored starting point.
  annotations:
    policy.datascape.io/exempt: |
      images-from-corp-registry: blueprint uses the default pinned image, not registry.corp.*
      require-digest-pins: blueprint's default image is tag-resolved at the runtime layer, not expressed as a digest in this manifest field
spec:
  type: debezium
  runtime:
    type: docker
  secretRefs: [ext-db-creds]
