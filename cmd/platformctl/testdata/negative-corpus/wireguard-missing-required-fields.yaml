# Class: Provider configuration fragment — multiple required fields missing
# (docs/adr/023, gate TunnelProvider, enabled by the test harness). Only
# peerNetwork is set; peerPublicKey/peerEndpoint/address/allowedIPs are all
# missing — previously only caught by wireguard.parseConfig at ValidateSpec
# time (still true), now also caught one layer earlier at schema-fragment
# validate (docs/planning/08 E5).
apiVersion: datascape.io/v1alpha1
kind: Provider
metadata:
  name: tun
spec:
  type: wireguard
  runtime: {type: fake}
  configuration:
    peerNetwork: "datascape"
